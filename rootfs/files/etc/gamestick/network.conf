# The recovery daemon owns interface discovery, retries, DHCP, and logging.
# Only this adapter-specific backend needs changing to replace wpa_supplicant.
WIFI_BACKEND=wpa_supplicant
WPA_CONF=/etc/wpa_supplicant/wpa_supplicant.conf
WIFI_MODULE=8188eu
RETRY_DELAY=5
ASSOC_TIMEOUT=45
DHCP_TRIES=8
